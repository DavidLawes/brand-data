export type Store = {
  id: string;
  brand_id: string;
  latitiude?: string; // sad face for spelling :(
  longitude?: string;
  website?: string;
  name?: string;
  description?: string;
  visible?: number;
  description_markdown?: string;
  image?: string;
  image_url?: string;
  latitude?: string;
};
